End game:
    set freq to every 10ms (val = 100)
    re-run ALL tests
        + cho_variant at slow
        + chow at slow for a short amount of time
    re-read code
    re-read design docs
    review config.mk

Submissions:
    copy to p3
    make -> run a bunch of tests
    make veryclean
    fill out hurdle paper form